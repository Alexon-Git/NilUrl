import React from 'react';
import "../../styles/Global/CreateLinkButton.css"

const CreateLinkButton = () => {
    return (
        <button className="CreateLinkButton custom-btn btn-8">
            <span>
                Создать ссылку
            </span>
        </button>
    );
};

export default CreateLinkButton;
